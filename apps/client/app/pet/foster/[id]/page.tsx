function NearAnimalApplyPage({ params }: { params: { id: number } }) {
  return (
    <main>
      <code>임시보호 신청하기 페이지 (ID: {params.id})</code>
    </main>
  );
}

export default NearAnimalApplyPage;
